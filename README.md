# Tarot_app_prof_MVP

Project skeleton for Tarot_app_prof_MVP.

Directory structure:
- spec/ - tests and specifications
- src/ - application source code
- public/ - static assets (images, CSS, JS)

Getting started:
- Add your source files to `src/`.
- Add tests/specs to `spec/`.
- Add static files to `public/`.

This commit creates the initial directories and README.
